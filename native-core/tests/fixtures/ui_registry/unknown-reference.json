{
  "schemaVersion": 1,
  "id": "registry-unknown-reference",
  "references": {
    "capabilities": ["missing.capability"],
    "commands": ["missing.command"],
    "components": ["missing.component"],
    "interactions": ["missingInteraction"],
    "results": ["missingResult"],
    "states": ["missing.state"],
    "values": ["missing.value"]
  }
}
