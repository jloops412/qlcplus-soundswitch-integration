{
  "schemaVersion": 1,
  "id": "registry-spike-binding",
  "references": {
    "capabilities": ["content.staticLooks"],
    "commands": [
      "show.start",
      "output.blackout.set",
      "staticLook.hold",
      "autoloop.launch",
      "group.override.property.set"
    ],
    "components": ["ember.activeLayers", "ember.autoloopMatrix", "ember.staticLookMatrix"],
    "interactions": ["absolute", "momentary", "trigger"],
    "results": ["accepted", "queueFull", "safetyRejected"],
    "states": [
      "runner.state",
      "output.blackout",
      "staticLook.active.id",
      "autoloop.active.progress",
      "output.micro.status"
    ],
    "values": ["target.staticLook", "unit.normalized", "value.adapterState", "value.fixtureProperty"]
  }
}
